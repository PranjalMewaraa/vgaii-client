# Dependencies & build output (reinstalled / rebuilt in the image)
node_modules
.next
out
build

# Prisma client is regenerated during the build
src/generated

# Local env & secrets — never bake these into the image
.env
.env.*

# VCS / tooling
.git
.gitignore
.claude
*.tsbuildinfo

# Docker files themselves
Dockerfile
.dockerignore
docker-compose*.yml

# Unrelated to the CRM build
hosted-site
client-sites
html-template
sales

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
